( function _Npm_ss_()
{

'use strict';

/**
 * File provider implements strategy for module files to access files over npm.
  @module Tools/mid/FilesNpm
*/

if( typeof module !== 'undefined' )
{
  let _ = require( '../../../../wtools/Tools.s' );
  require( '../include/Npm.ss' )
  module[ 'exports' ] = _;
}

})();
